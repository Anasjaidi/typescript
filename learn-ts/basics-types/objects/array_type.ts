const person3 = {
	name: "new",
	age: 12,
	hobbies: ["coding"], // hobbies: string[]

};

// const my_arr: string[]
const my_arr = ["anas"]

let new_arr: string[] = [];

new_arr.push("anas")

for (const str of new_arr) {
  console.log(str.substring(0, 5));
  //! console.log(str.map()); ERROR !!!!!!
  
  
}