const obj : {
  name: string;
  age: number;
} = {
  name : 'anasjaidi',
  age :  19
}

const arr : string[] = ['anas jaidi', 'mqwed']
const arr2 : (string | number)[] = ['anas jaidi', 'mqwed', 12]

const ca :  12 = 12

const tuple : [number, string] = [19, "anas jaidi"]
tuple.push(12) // pushing to tuples with fixed siare valid 


enum An { // dot notation
  v1,
  v2
}